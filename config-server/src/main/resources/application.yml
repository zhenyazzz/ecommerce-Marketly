server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhenyazzz/config-repo
          default-label: main
  application:
    name: config-server

logging:
  level:
    org.springframework.cloud.config.server: DEBUG